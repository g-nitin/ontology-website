# The file contains some helpful commands for when developing the website.

```shell
bundle install
bundle update
bundle add webrick
bundle exec jekyll serve --baseurl="/"
```
https://plethora.debian.net/viewtopic.php?t=153167


```shell
git push -u origin gh-pages
```